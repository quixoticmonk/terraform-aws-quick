# Minimal example

Stands up only `aws_quicksight_account_settings`. Every opt-in flag is off.

```bash
cp terraform.tfvars.example terraform.tfvars
# edit terraform.tfvars
terraform init
terraform plan
```
